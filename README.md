# Simple Currency Conversion tool

Allows you to select currency pairs to convert between. The tool works in one direction only to keep things simple.

## Setup guide

1. update the api key in `src/lib/api.ts`
2. run `npm install`
3. run `npm run dev`
4. visit the app on `http://localhost:5173/`
